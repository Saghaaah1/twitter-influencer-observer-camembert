# raw competition data — keep locally in data/, never commit (1.1 GB)
*.jsonl
data/*.jsonl

# generated artifacts: submissions, OOF/test probability arrays, embeddings
*.csv
*.npy
output/runs/

# preserved experiment history — kept on disk, out of the tracked tree
archive/

# python / notebooks
__pycache__/
*.pyc
.ipynb_checkpoints/

# editor / local tooling
.vscode/
.claude/

# stray logs
*.log
