# Influencer or Observer 

# Install with:  pip install -r requirements-lock.txt

# NOTE: torch is pinned to the CUDA 12.6 build. On a CPU-only machine, drop the
# "+cu126" suffix (pip install torch==2.12.0) or install from https://pytorch.org.

numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.8.0
lightgbm==4.6.0
catboost==1.2.10
xgboost==3.2.0
torch==2.12.0+cu126
transformers==5.10.2
tensorboard==2.20.0
matplotlib==3.10.9
nbformat==5.10.4
